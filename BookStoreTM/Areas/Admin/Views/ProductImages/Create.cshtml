@* @model BookStoreTM.Models.EF.ProductImageViewModel *@
@model BookStoreTM.Models.ProductImage

@{
    ViewData["Title"] = "Create";
    Layout = "~/Areas/Admin/Views/Shared/_LayoutAdmin.cshtml";
    var productId = ViewBag.Id;
}

<form asp-action="Create" enctype="multipart/form-data" method="post">
    <input type="hidden" name="productId" value="@productId" />
    <div class="form-group">
        <label class="control-label">Tải ảnh</label>
        <input class="form-control" type="file" name="productImages" multiple />
    </div>
    <div class="form-group">
        <input type="submit" value="Tải lên" class="btn btn-primary" />
    </div>
</form>

@* <form asp-action="Create" enctype="multipart/form-data" method="post">
    <input type="hidden"  name="productId" value="@productId" />
    <div class="form-group">
        <label asp-for="Url" class="control-label">Tải ảnh</label>
        <input asp-for="Url" class="form-control" type="file" multiple />
    </div>
    <div class="form-group">
        <input type="submit" value="Tải lên" class="btn btn-primary" />
    </div>
</form> *@

